# M1 native transfer evidence: September 23, 2026

The frozen M1 campaign passed its selected native engineering comparison on macOS 26.5.1, build 25F80.
Installed body-only and canonical arms produced identical original sensor payload bytes for all 24 body ticks.
Readback reconstructed requests from the original workload and joined canonical commands, responses, payloads, acknowledgements, and terminal records.

The [machine-readable summary](native-m1-2026-09-23.json) records source identities, artifact digests, case outcomes, and rejection reasons.
The [campaign owner](../M1_CAMPAIGN.md) defines the maintained execution and verification commands.

## Frozen case and accounting

The [original workload](../tests/fixtures/m1.workload.v1.json) selects one force-ground body, no solids, RGB, thermal, and acoustic pressure.
Its 3,444 bytes have SHA-256 `f4b8cad4a9daf1f5b95abb9b03409040cd8caa67cf2e14c074a17dcc4a77ecae`.
The ordered workload targets apply through the public body contract.
The comparator independently reconstructs those requests for each arm.

| Observed quantity | Count |
| --- | ---: |
| Completed body ticks | 24 |
| Complete payloads | 44 |
| Raw payload bytes | 4,326,400 |
| Pressure samples | 3,200 |
| Payload chunks | 168 |
| NCP exchanges | 476 |
| Maximum exchanges in one call | 36 |
| Canonical calls | 26 |
| Canonical events | 82 |

The canonical capture contains 6,670,005 bytes, and its command log contains 55,442 bytes.
Raw payload bytes, capture storage, and canonical storage are separate quantities.
These counts establish accounting for this case, without a performance or supported-scale claim.

## Healthy and fault outcomes

All four outer commands exited zero after verifying their expected case conditions.
The two fault workers remained unhealthy.
Observed process retirement did not replace either fault's application cleanup result.

| Case | Worker exit | Application cleanup | Observed identities retired | Capture outcome |
| --- | ---: | --- | ---: | --- |
| Body-only healthy | 0 | `true` | 15 of 15 | Complete original wire record |
| Canonical healthy | 0 | `true` | 15 of 15 | Finalized capture and canonical log |
| Canonical caller loss | −9 | `null`: unavailable | 14 of 14 | `terminal_missing` |
| Canonical renderer loss | 1 | `false` | 15 of 15 | `terminal_missing` |

Caller loss sent `SIGKILL` to the directly owned worker after successful Prepare.
Renderer loss sent one version-bound `SIGTERM` after one validated tick.
Its target was the selected main browser owner with its observed Node parent.
The renderer-loss worker retained its original session and cleanup failures.

Neither fault has a successful Finish or an authenticated complete capture.
The external observer sent no signals, and no case required emergency cleanup.
The process claim covers observed identities, including observed zombies until disappearance.
It does not establish exhaustive descendant tracking or hostile-process containment.

## Rejecting controls and independent readback

Two copied-artifact controls rejected a changed command and a changed capture terminal.
Thirteen provenance controls rejected stale selections, mixed wheels or environments, wrong identities, changed ownership, and an altered workload.
Both healthy arms rejected the same altered workload during original-request reconstruction.
The controls retained their selected failure reasons and reverified the original bytes afterward.

A separate council reopened 196 inputs without launching a simulator or sending signals.
It reran the maintained healthy comparator and both fault verifiers.
It also rejoined the 15 retained negative-control results, without rerunning those mutations.
All reopened inputs remained unchanged.
This independent review uses the maintained comparator and supplies no independent renderer oracle or scientific replication.

## Source, custody, and limits

The native campaign selected Prisoma commit `d6a0cc3ca84effb0adfa21cbdf22f783ebc970b1`.
Both arms installed the same CREBAIN client from `a5037a23a8e55d39ca0f09da2c25853c5209467b` and NCP SDK from `c0465d40f1f2b9df2caf9793183d11e65ac9ec74`.
The native CREBAIN runtime separately identifies source `d397905fe51c687b229a496af3b7dac572595e55`.
These client and runtime identities are distinct.
The body-only environment excludes Prisoma, NEST, and NumPy.

The maintained source, original workload, and this summary are public.
Original captures, inventories, process journals, command receipts, and council readback remain private, locally observed artifacts.
Their hashes identify retained bytes, without producer authentication or loaded-code attestation.
A public summary does not replace those original artifacts for independent replay.

This campaign establishes selected transfer and failure behavior.
It supplies no learned forecast, restored-branch label, statistical validation, physical calibration, controller-stability result, or real-time guarantee.
Many-entity qualification, the complete application bootstrap, and full release gates remain separate.
The [September 10 receipt](native-2026-09-10.json) preserves its historical eight-command, 1,542,400-byte case and original source identities.
