# Agent-tool working directories (workflow scripts, scratch)
.claude/
.devin/
.playwright-mcp/

# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
*.dSYM/

# Python
__pycache__/
*.py[cod]
*.pyd
*.so
.env
.env.local
.env.*.local
.envrc
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Local experiment outputs (keep config/scripts in git; keep raw outputs out)
data/
results/
logs/
*.log
outputs/*
!outputs/arxiv_ref_cache.json

# UI prompt-iteration artifacts (generated images/reviews; keep specs/scripts in git)
uidesigner/out/

# Editors/IDEs
.idea/
.vscode/

# Node / Tauri / frontend tooling
node_modules/
dist/
build/
.parcel-cache/
.turbo/
.next/
out/

# Nix / direnv
result
result-*
.direnv/

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Local scratch space for external reference repos (e.g., for audits/comparisons).
.external/

# Local one-off tooling / report generation artifacts (keep out of the main repo unless promoted).
report_gen.py

# Meshmaker is quarantined out of the canonical repo (see meshmaker/README.md).
# It is cost-bearing external asset-generation tooling unrelated to the prisoma
# scientific core; only the tombstone README is tracked. Local files are kept on
# disk (untracked), so nothing is deleted by the quarantine.
meshmaker/*
!meshmaker/README.md
